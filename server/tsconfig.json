{
  "compilerOptions": {
    "module": "commonjs",
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "target": "es6",
    "noImplicitAny": true,
    "moduleResolution": "node",
    "sourceMap": true,
    "outDir": "dist",
    "baseUrl": "./server"
  }
}

// run with this : tsc -p tsconfig.json
// or only with tsc 
  
// "start": "cd client && npm install && npm run build && cd .. && cd server && npm install && tsc && node dist/index.js"
// when we connect the FE
